%h2 Zarządzanie stronami

.actions
  =link_to "Dodaj nową stronę", new_admin_page_path, class: 'button medium radius brown'

%table
  %thead
    %tr
      %th Nazwa
      %th Slug
      %th Aktywna?
      %th W nawigacji?
      %th Ostatnie zmiany
      %th
      %th
  %tbody
    - @pages.each do |page|
      %tr
        %td= page.name
        %td= page.slug
        %td= "TAK" if page.active?
        %td= "TAK" if page.show_in_navbar?
        %td= l page.updated_at.to_date, format: :long
        %td= link_to "edycja / treść", edit_admin_page_path(page)
        %td= button_to "usuń", admin_page_path(page), method: :delete, confirm: "Na pewno usunąć tę stronę?"
