%h2 Zarządzanie użytkownikami

%table
  %thead
    %tr
      %th Imię
      %th Nazwisko
      %th Adres email
      %th Admin?
      %th Konto założone
      %th
      %th
  %tbody
    - @users.each do |user|
      %tr
        %td= user.name
        %td= user.lastname
        %td= user.email
        %td= "TAK" if user.is_admin?
        %td= l user.created_at.to_date, format: :long
        %td= link_to "edycja / treść", edit_admin_user_path(user)
        %td= button_to "usuń", admin_page_path(user), method: :delete, confirm: "Na pewno usunąć tę stronę?"

