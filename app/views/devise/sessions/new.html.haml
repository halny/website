%h2 Zaloguj się

= semantic_form_for resource, as: resource_name, url: session_path(resource_name) do |f|
  = f.inputs do
    = f.input :email, label: 'Adres email', as: :email
    = f.input :password, label: 'Hasło', as: :password
    - if devise_mapping.rememberable?
      = f.input :remember_me, label: 'Zapamiętaj mnie', as: :boolean

  = f.buttons do
    = f.commit_button "Zaloguj się", button_html: { class: 'button medium brown radius' }

= render partial: "devise/shared/links"
